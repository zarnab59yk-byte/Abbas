name: Ubuntu Server

on:
  workflow_dispatch:  # دستی اجرا میشه

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Start tmate session
      uses: mxschmitt/action-tmate@v3
      with:
        limit-access-to-actor: true  # فقط خودت دسترسی داری
